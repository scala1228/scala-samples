object Main extends App {
    
    var arr=Array(20,30,10,50)
    var sum=0
    
    for(i<- 0 to arr.length-1)
    {
        sum=sum+arr(i)
    }
    println(sum)
    
    var avg=sum/arr.length
    
    println(avg)
    
}
